import 'package:flutter/material.dart';

class FiltroAnimais extends StatelessWidget {
  const FiltroAnimais({super.key});
  @override
  Widget build(BuildContext context) {
    List<String> _tipos = ['todos', 'Cachorro', 'Gato', 'Papagaio'];

    return Container();
  }
}
